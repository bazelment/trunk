# Root package of the trunk repo.
#
# Bazel requires the repository root to be a package for some module
# extensions (e.g. rules_rust crate_universe) that resolve `@@//:MODULE.bazel`.

package(default_visibility = ["//visibility:public"])

exports_files(["MODULE.bazel"])
